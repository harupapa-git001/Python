最初にModule.pyから開いてください。

モジュールのimportについて理解ができます。

次にutil.pyとutil_main.pyを同じディレクトリに用意し、util_main.pyからutil.pyを呼び出すことで構造について理解できます。

さらに、同じディレクトリにReload_main.pyとLodingAndExecuting_main.pyを追加してそれぞれ実行してください。

util.pyに記載されたコードを呼び出す際の処理に関して理解できます。

次にしたのみっつのファイルを同じディレクトリに用意し、hello.pyから実行してください。

    hello.py(実行ファイル)
    nice.py(呼び出しファイル)
    world.py(呼び出しファイル)

モジュールの読み込み時についての変数__name__と__main__について理解できます。

最後にtest.pyを実行しながらOSモジュールについて理解してください。